spring:
  application:
    name: ms-usuario
  cloud:
    config:
      enabled: true
      uri: http://localhost:8084

  security:
    aouth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/auth/realms/fidelin-realm
