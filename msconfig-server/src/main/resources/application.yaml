server:
  port: 8084
spring:
  cloud:
    config:
      server:
        git:
          default-label: keycloak
          uri: https://github.com/fidelin17/microservicios
          search-paths: config-data
  application:
    name: msconfig-server
